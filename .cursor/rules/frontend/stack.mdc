---
description: Preferred frontend technology stack and library usage patterns
globs: "apps/web/**/*,packages/ui/**/*,packages/react/**/*"
alwaysApply: false
---

See @apps/docu/content/docs/architecture/frontend.mdx for complete documentation

## Core Libraries
- `@tanstack/react-query`: Data fetching/async (use `@lukemorales/query-key-factory`)
- `@repo/react`: React Query hooks and helpers only—never UI components. Route-specific UI (e.g. login form) lives in apps, collocated by route
- `nuqs`: URL-based state (search/filters/tabs/pagination)
- `zod`: Schema validation
- `@repo/lib`: Utility library
- `lodash-es`: Common operations (per-function imports)
- `ahooks`: `useSetState` (grouped state), `useLocalStorageState` (localStorage)

## Essential Patterns
Query Key Factory (see @.cursor/rules/frontend/react-hooks.mdc), nuqs for URL-shareable state, `@repo/core` for API clients. Auth: see @.cursor/rules/frontend/auth.mdc
