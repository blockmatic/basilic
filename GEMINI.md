Read and follow root `AGENTS.md`. That file is the always-on agent contract.

Project skills live in `.agents/skills/` after `pnpm setup`. Recommended daily workflow is mattpocock/skills. Basilic playbooks are `.agents/skills/w-<name>/SKILL.md`.
