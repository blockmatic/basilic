Read and follow root `AGENTS.md`. That file is the always-on agent contract.

Project skills live in `.agents/skills/` after `pnpm setup`. Workflow playbooks are `.agents/skills/workflow/` (leaf `SKILL.md`, including group folders).
