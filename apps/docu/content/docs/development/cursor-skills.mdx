---
title: "Skills"
description: "Tech, pattern, and workflow skills from the Basilic skills.sh catalog. Any agent that can read .agents/skills/."
---

Project skills live under `.agents/skills/` after `pnpm setup` (or `pnpm setup:skills`). That path is gitignored. `skills-lock.json` must pin **only** `blockmatic/basilic-skills` (including vendored `react-email`). Setup runs one `skills add blockmatic/basilic-skills --skill '*'` (one Git clone), then writes the lock file back so hashes stay pinned. It fails on a second GitHub catalog or on `sourceType: local` unless `BASILIC_SKILLS_LOCAL=1` (then it adds `../basilic-skills`). Do not use `skills experimental_install`. To refresh hashes, run `skills add` yourself and commit the lock.

The [skills CLI](https://github.com/vercel-labs/skills) writes the tree for Cursor, Codex, Antigravity, Gemini CLI, Copilot, and any agent that reads Agent Skills. This repo’s install is `-a cursor --copy` into `.agents/skills/` (Codex shares that path). Claude Code’s default dir is `.claude/skills/`; do not `--copy` that tree into git. `CLAUDE.md` tells Claude to read `.agents/skills/` instead.

Cursor also reads `.cursor/skills/` if present; this repo does not commit that tree. Cursor requires `name` in each `SKILL.md` to match the folder (`fastify-v5`, not `fastify`).

Rules override skills. Skills override the model. Indexed docs are advisory. Daily playbooks: [AI Workflow](/docs/development/ai-workflow). Optional Cursor extras: [Agent setup](/docs/development/cursor-setup).

## Catalogs

**Tech, patterns, and workflow** — [blockmatic/basilic-skills](https://github.com/blockmatic/basilic-skills) on [skills.sh](https://skills.sh/blockmatic/basilic-skills). Playbooks install as the parent `workflow` skill. `react-email` is vendored in that catalog (upstream [resend/react-email](https://github.com/resend/react-email)).

Interactive pick (skills and agents):

```bash
pnpm dlx skills@latest add blockmatic/basilic-skills
```

Non-interactive install for this repo is `pnpm setup:skills` (Update procedure below). Full installer options: catalog [README](https://github.com/blockmatic/basilic-skills#agents-and-paths). Optional maintainer-local Claude symlink (not committed): add `-a claude-code` **without** `--copy`.

Root `skills-lock.json` tracks **skill-file** hashes for this catalog. The pinned `web-interface-guidelines` `command.md` URL and sha256 live in the catalog `web-design-guidelines` skill, not as an extra lock field. `pnpm setup:skills` restores the lock after install so routine setup does not bump hashes. Commit the lock after an explicit catalog refresh. Do not commit `.agents/skills/`.

## Three kinds

**Tech** — `.agents/skills/<topic>-v<major>/` (example: `fastify-v5`). Agent-invocable. Required sections: Scope, Assumptions, Principles, Constraints, Interactions. Force with `@.agents/skills/<topic>-v<major>` or `/<topic>-v<major>`.

**Patterns** — unversioned craft and guidelines (example: `composition-patterns`, `frontend-design`, `vercel-react`). Agent-invocable. Same loading as tech skills.

**Workflow** — install the parent `workflow` skill; nested children are not independent install targets. Everyday leaves live at `.agents/skills/workflow/<name>/`; specialists may be `.agents/skills/workflow/<group>/<name>/` (example: `workflow/git/commit`). Slash is the leaf name. Set `disable-model-invocation: true`. One-line `description`. Not versioned. Type `/plan`, `/build`, or `/review` in Cursor. If a harness has no matching menu, read the `SKILL.md` file directly.

`name` is lowercase letters, digits, and hyphens, max 64 characters, equal to the folder name.

Skill descriptions are always in the agent’s discovery context. One folder per skill. Do not keep `foo/` next to `foo-v1/`. Prefer fewer, focused skills.

## Workflow install

The published package is `workflow`: `.agents/skills/workflow/SKILL.md` is the parent catalog for **42** child playbooks (including `/plan`, `/build`, `/review`, `/ui`, and `/shadcn`) plus three references. A full `--skill '*'` install is **45** top-level skills (`skills.sh.json` groupings); do not treat that catalog-wide count as the playbook count. Shared `authoring.md`, `git-publish.md`, and `review-dimensions.md` live under `workflow/references/`. Playbook bodies are numbered instructions. The parent lists shortcuts and stops; extra tokens (`/workflow plan`) do not dispatch.

This repository should have one workflow tree. Invoke playbooks as `/<name>`. After a refresh, preserve local edits first, then run `pnpm dlx skills@latest remove --skill b -a cursor -y` if a leftover `b` tree appears. `.agents/skills/b/` must be absent afterward.

Packaging validation runs in the catalog with `pnpm validate`. It checks metadata, counts, grouping, and workflow reference targets. These checks do not prove that an agent follows a playbook or that a particular desktop menu has refreshed.

Installable names follow catalog `skills.sh.json` (45): Core (`typescript-v6`, `file-organization`); Workflow (`workflow`); Web (`next-v16`, `vercel-react`, `tanstack-query-v5`, `ahooks-v3`, `nuqs-v2`, `react-email`); UI (`shadcn-v3`, `tailwind-design-system-v4`, `frontend-design`, `web-design-guidelines`, `composition-patterns`, `better-ui`, `motion-v13`, `emilkowal-animations`, plus the Emil craft folders listed below); API (`fastify-v5`, `drizzle-orm-v0`, `openapi-ts-v0`); AI (`ai-sdk-core-v7`, `ai-sdk-ui-v7`); Expo (`expo-*-v55`, `animate-expo`); Web3 (`wagmi-v3`, `solana-v1`, `viem-v2`, `nodejs-keccak256-v1`); Testing (`vitest-v4`, `playwright-v1`).

## Versioning

`<topic>-v<major>` is the **library or SDK major**, not a skill-file revision.

- Library major change → new folder (`next-v17`). Drop the old folder when the repo no longer uses that major.
- Upstream wording, references, or bugfixes → edit the catalog; re-run the skills CLI in this repo.
- Pattern, guideline, and craft skills stay unversioned (`frontend-design`, `emilkowal-animations`, `vercel-react`). Library skills (including Motion) use the npm major (`motion-v13` for `motion` ^13).
- Workflows stay unversioned.

This repo’s Expo app is SDK 57; Expo skills are still `*-v55` because that is what upstream publishes. Do not invent empty `*-v57` folders. When Expo ships v57 skills, add those folders in the catalog and drop v55.

## Update procedure

Install the tree without changing the lock:

```bash
pnpm setup:skills
```

Maintainer preview of a sibling catalog clone:

```bash
BASILIC_SKILLS_LOCAL=1 pnpm setup:skills
```

Refresh from GitHub (rewrites hashes in `skills-lock.json`; review and commit the lock). The catalog README also documents `pnpm dlx skills@latest update`. Do not run `pnpm setup:skills` afterward if you intend to keep the new hashes — that script restores the previous lock.

```bash
pnpm dlx skills@latest add blockmatic/basilic-skills --skill '*' -a cursor --copy -y
```

A named refresh is valid (`--skill next-v16`, `--skill workflow`). After a refresh, run `pnpm dlx skills@latest remove --skill b -a cursor -y` if `.agents/skills/b/` is present. That leftover path must be absent afterward.

Review the lockfile diff; commit `skills-lock.json` only. Do not reformat skill copies to match this repo's Biome rules; `biome.json` excludes `.agents/skills/**`.

Do not `git pull` inside nested `.git` under skills. Do not store project skills only in `~/.cursor/skills/`.

Repo-specific overlays stay in `.cursor/rules/` (example: Playwright app E2E paths in `e2e-playwright.mdc`).

When grepping skill names, match the folder or `name:` exactly (`motion-v1/`, `name: motion-v1`). Do not use a substring (`motion-v1` also hits `motion-v13`).

## Upstream provenance

Catalog copies may differ from upstream. Original sources (same list as the catalog [README](https://github.com/blockmatic/basilic-skills#canonical-copies)):

- `expo-cicd-workflows-v55` ← `eas-workflows` in [expo/skills](https://github.com/expo/skills)
- `expo-deployment-v55` ← `eas-app-stores`
- `expo-dev-client-v55` ← `expo-dev-client`
- `expo-tailwind-setup-v55` ← `expo-tailwind-setup`
- `expo-upgrading-v55` ← `expo-upgrade`
- `expo-use-dom-v55` ← `expo-dom`
- `vercel-react` ← `react-best-practices` in [vercel-labs/agent-skills](https://github.com/vercel-labs/agent-skills)
- `web-design-guidelines` ← `web-design-guidelines` in [vercel-labs/agent-skills](https://github.com/vercel-labs/agent-skills) (fetches a pinned [web-interface-guidelines `command.md`](https://github.com/vercel-labs/web-interface-guidelines/blob/e3d624baaf29dc1fc645aff3e38f03e564d2d6b1/command.md) revision)
- `composition-patterns` ← `composition-patterns` in [vercel-labs/agent-skills](https://github.com/vercel-labs/agent-skills)
- `next-v16` ← `nextjs` in [pproenca/dot-skills](https://github.com/pproenca/dot-skills)
- `nuqs-v2` ← `nuqs` in [pproenca/dot-skills](https://github.com/pproenca/dot-skills)
- `vitest-v4` ← `vitest` in [pproenca/dot-skills](https://github.com/pproenca/dot-skills)
- `viem-v2` ← `viem-integration` in [uniswap/uniswap-ai](https://github.com/uniswap/uniswap-ai)
- `nodejs-keccak256-v1` ← `nodejs-keccak256` in [affaan-m/ecc](https://github.com/affaan-m/ecc)
- `frontend-design` ← [anthropics/skills](https://github.com/anthropics/skills) `frontend-design`
- `emilkowal-animations` ← pproenca/dot-skills `emilkowal-animations`
- [emilkowalski/skills](https://github.com/emilkowalski/skills) at [`85e8e236`](https://github.com/emilkowalski/skills/commit/85e8e2363b713506e1d5b6e07a0eb2da66be1bc3): `emil-design-eng`, `review-animations`, `animation-vocabulary`, `apple-design`, `improve-animations`, `find-animation-opportunities`, `pick-ui-library`, `prototype`, `animate`, `ask-sonner`, `animate-expo`
- `better-ui` ← [jakubkrehel/skills](https://github.com/jakubkrehel/skills) at [`267330e1`](https://github.com/jakubkrehel/skills/commit/267330e1adfc66a718fb65fa6918c1f06d0a689e) (sibling `better-*` skills are not in the catalog)
- `react-email` ← [resend/react-email](https://github.com/resend/react-email) `skills/react-email`

Basilic-maintained in the catalog: `fastify-v5`, `file-organization`, `wagmi-v3`, `solana-v1`, `ahooks-v3`, `ai-sdk-core-v7`, `ai-sdk-ui-v7`, `drizzle-orm-v0`, `motion-v13`, `openapi-ts-v0`, `playwright-v1`, `shadcn-v3`, `tailwind-design-system-v4`, `tanstack-query-v5`, `typescript-v6`, and all workflow playbooks.

This product uses Base UI (`base-vega`, `render`, `data-slot` / `data-open`). Published GitHub `shadcn-v3` and `/shadcn` still describe Radix until that catalog change is merged. Preview the sibling clone with `BASILIC_SKILLS_LOCAL=1 pnpm setup:skills`. After the catalog ships, refresh hashes with `skills add` (or `skills update`) and commit `skills-lock.json`.

**Intended unused stack** (copied by a full `--skill '*'` refresh, not installed app deps): `wagmi-v3`, `motion-v13`, `emilkowal-animations`, `pick-ui-library`, `better-ui`. `ask-sonner` matches `@repo/ui` Sonner.

**Omitted from the catalog** (and why): Vercel `writing-guidelines` (conflicts with docs sentence case); `react-view-transitions` (extra animation API); `react-native-guidelines` (Expo skills already cover native); `write-swift` (no Swift in this product); `mobile-native` (web PWA chrome; Expo skills cover native). [Impeccable](https://github.com/pbakaus/impeccable) (Apache 2.0) is not a skill: competing PRODUCT/DESIGN generators, CLI, and detector hooks are out of scope. A few craft ideas (harden loading/empty/error/success; bounded screenshot critique) are adapted in original wording in `/ui` and `frontend-design` `references/product-ui.md`.

## Design

UI craft also comes from [emilkowalski/skills](https://github.com/emilkowalski/skills). Those folders sit next to tech and pattern skills, not under `workflow/`. Project tokens in `DESIGN.md` still win.

New or reshaped UI: `/ui`. New shadcn primitive: `/shadcn` (follow `shadcn-v3`). Visual direction only: `@frontend-design` (tokens win when present). Component APIs: `@composition-patterns`. UI code audit: `@web-design-guidelines`. Quality a11y report: `/a11y`. Runtime bugs: `/debug-browser`. Explore variants: `/prototype`. Motion: `@animate` (web) or `@animate-expo` (native). Toasts: `@ask-sonner`. Do not run `@pick-ui-library` to replace shadcn in this repo.

## Related

- [AI Workflow](/docs/development/ai-workflow)
- [Agent setup](/docs/development/cursor-setup)
- Repo [`.cursor/README.md`](https://github.com/blockmatic/basilic/blob/main/.cursor/README.md)
- [Cursor skills](https://cursor.com/docs/context/skills)
- Catalog [README](https://github.com/blockmatic/basilic-skills#canonical-copies)
