---
title: After fork
description: Generator vs fork, Product Ready, and what to replace when the copy is your product.
---

Basilic stays the starter brand. **Start a product** with `npx create-basilic@latest my-app` so the new repo has no Basilic git history, no `apps/docu`, and no generator. **Fork** (or clone) this repository only to collab on Basilic. GitHub **Use this template** is not a product start path. First run is still [Product Ready](/docs/testing/product-ready). There is no rename script and no upgrade CLI.

## Generator vs fork

- **`npx create-basilic@latest`** — start a product. Independent git history; no docu app; no generator; local docs snapshot in `docs/basilic/`.
- **Fork** — contribute back to Basilic. Keep the upstream remote.
- **Use this template / clone of this repo** — not a product start path. The copy includes the documentation app and generator.

## Run

Follow [Getting Started](/docs/development): `pnpm setup`, `db:start`, `pnpm reset`, `pnpm dev`. Copied env sets `ALLOW_TEST=true`. Sign in with `test@test.ai`. Production refuses `ALLOW_TEST`.

Fastify `POST /ai/generate` is optional API chrome. Leave Anthropic unset so Ollama can be the free path, or set a real key. Do not use a truthy placeholder. Dashboard Chat is eve, not Fastify.

## Replace when you ship as you

Keep Basilic names until you care. Then:

- `APP_NAME`, `WEB_APP_URL`, `EMAIL_FROM` in API env
- OAuth client ids (unconfigured providers stay 503)
- [`.cursor/mcp.json`](https://github.com/blockmatic/basilic/blob/main/.cursor/mcp.json) Vercel MCP URLs (`mcp.vercel.com/gaboesquivel/basilic-*`) — comment them out until you have your own projects
- Mobile `appId` `com.blockmatic.basilic`
- DeepSec `githubUrl` in `.deepsec/deepsec.config.ts`

Do not sweep every `blockmatic` docs URL. Those point at the upstream starter.

## After you own the copy

Existing copies that still have `_first/` can delete that folder. The agent contract (`AGENTS.md`, `.agents/rules/always.md`) ships with the copy; keep it pointing at local docs (`docs/basilic/`).

## CI secrets

Lint, `security.yml`, package tests, and API unit/Scalar E2E run without org secrets. Fork PRs do not get upstream secrets.

- **DeepSec** already skips untrusted and fork PRs.
- Vercel and EAS are not Product Ready.

## License

MIT. Root [`LICENSE`](https://github.com/blockmatic/basilic/blob/main/LICENSE). Keep the copyright notice in copies.

## Related

- [Product Ready](/docs/testing/product-ready)
- [Getting Started](/docs/development)
- [GitHub Actions](/docs/deployment/github-actions)
