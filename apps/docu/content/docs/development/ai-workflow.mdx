---
title: "AI Development Workflow"
description: "AGENTS.md plus a catalog of skills: tech, patterns, and slash playbooks for any coding agent or IDE."
---

Root `AGENTS.md` plus `.agents/skills/` (after `pnpm setup`) is the contract: versioned **tech** skills, unversioned **patterns**, and **workflow** playbooks (`/plan`, `/build`, `/review`, `/test`, `/pr`). Any agent or IDE that can read those files. If the harness has no `/` menu, open the `SKILL.md`. Do not create a parallel process.

Cursor slash (`/plan` `/build` `/review`), glob auto-attach, and `.cursor/mcp.json` are **adapters**, not the contract. Claude Code, Codex, Gemini CLI, and others use the same tree. Catalog, install flags, and the Emil design pack: [Skills](/docs/development/cursor-skills). Optional Cursor indexing/MCP: [Agent setup](/docs/development/cursor-setup).

Judgment lives in planning and review. Implementation is incremental. See [Engineering in the AI Era](https://gaboesquivel.com/blog/2026-02-engineering-ai-era).

Durable product intent lives in root `PRODUCT.md`. Visual language is root `DESIGN.md`. Technical adopter docs live in `apps/docu`. Installed playbooks come from [`blockmatic/basilic-skills`](https://github.com/blockmatic/basilic-skills) under `.agents/skills/workflow/`. `/tdd` is opt-in. In-app chat is the [demo assistant](/docs/architecture/ai), not this page.

## Sources of truth

Root `AGENTS.md` holds always-on constraints (behavior, naming, file organization, git, GitHub Actions). Glob `.mdc` files under `.cursor/rules/` are file-scoped constraints; Cursor auto-attaches them. **Read** the matching MDX for the task. Do not `@`-attach MDX from rules or skills.

## Guidance hierarchy

| Layer | Role | How it loads |
| --- | --- | --- |
| **AGENTS.md** | Always-on contract | Cursor and Codex read it; Claude via `@AGENTS.md` in `CLAUDE.md`; Gemini CLI via `GEMINI.md`; Antigravity via `.agents/rules/always.md` |
| **Glob rules** (`.cursor/rules/*.mdc`) | File-scoped constraints | Cursor attaches by glob. Other harnesses read the matching file when those paths are in scope (table in `AGENTS.md`) |
| **Workflow skills** (`.agents/skills/workflow/`) | Playbooks | Cursor: `/<leaf>`. Otherwise open `SKILL.md` |
| **Tech skills** (`.agents/skills/<topic>-v<major>/`) | Stack patterns | Agent loads when relevant; `@` or `/` to force |
| **Pattern skills** (unversioned) | Craft and guidelines | Agent loads when relevant; `@` or `/` to force |
| **Indexed docs** | Advisory | Never override `AGENTS.md`, glob rules, or source |

Cursor slash menus, `.cursor/mcp.json`, and glob auto-attach are **Cursor extras**, not the portable contract.

## Slash playbooks

In Cursor chat, type `/` then the skill name.

- **Enter** — attach for this message only
- **Option+Enter** (Alt+Enter) — pin for the session

Invoke playbooks directly. `/workflow` lists the catalog and stops; extra tokens (`/workflow plan`) do not dispatch.

| Task | Invocation | Folder |
| --- | --- | --- |
| Plan a feature | `/plan` | `plan` |
| Implement and verify locally | `/build` | `build` |
| Review a change | `/review` | `review` |
| Diagnose or fix a failure | `/debug` | `debug` |
| Run tests and fix failures | `/test` | `test` |
| Commit intended changes | `/commit` | `git/commit` |
| Push the intended branch | `/push` | `git/push` |
| Create a described PR | `/pr` | `git/pr` |
| Implement through a described PR | `/ship` | `git/ship` |
| Reflect on completed work | `/retro` | `retro` |
| Build or reshape UI | `/ui` | `ui` |
| Explore UI variants | `/prototype` | `prototype` (Emil pack, not under `workflow/`) |

`/build` ends with verified local changes. `/ship` requests implementation, full validation, commit, push, and a described PR. No playbook implies merge or deployment. Specialists include `/diagram`, `/docs`, `/git-repair`, and `/coderabbit` (PR review playbook; CodeRabbit MCP is [Agent setup](/docs/development/cursor-setup)). `/workflow` lists the rest and stops.

Slash names are leaf folders. There are no alias playbooks. Removed names map as follows:

| Removed | Current |
| --- | --- |
| `/git-commit` | `/commit` |
| `/git-push` | `/push` |
| `/git-create-pr` | `/pr` |
| `/exec-push` | `/ship` |
| `/use-frontend` | `/ui` |
| `/use-tdd` | `/tdd` |
| `/plan-architecture` | `/architecture` |
| `/refactor-code` | `/refactor` |
| `/overview` | `/diagram` |
| `/docker` | `/debug` |
| `/info` | `/docs` |
| `/issues` | `/git-repair` |
| `/rabbit` | `/coderabbit` |

When a harness does not expose the slash menu, read the `SKILL.md` under `.agents/skills/workflow/`. Do not create another skills tree. Do not commit `.claude/skills/` copies.

## Typical order

Compose skills. This is not a router.

1. **Plan** — `/plan` for non-trivial work. Read existing implementation and docs; write Goals, Assumptions (3–5), Tasks, Risks, and References (plain paths). Planning alone does not create a branch or implement code.
2. **Review the plan** — `/review-plan` checks scope and ordering. Resolve consequential product or architecture decisions from `PRODUCT.md` and matching MDX/ADRs; continue routine work already authorized.
3. **Implement** — `/build` delivers complete slices and runs affected checks. Preserve unrelated changes and generated-source ownership. Update matching docs when behavior changes. Update `PRODUCT.md` only when product facts change.
4. **Review the change** — `/review` returns evidenced findings and verification limits. Reviews stay read-only unless fixes are requested.
5. **Publish when requested** — `/commit`, `/push`, or `/pr` perform their named actions. `/ship` follows installed `git-publish.md`: dirty tree stops (no stash); `/commit` may still take task-owned hunks and is the only playbook that owns Conventional Commit format (composers must not paraphrase it). `/ship` requires `pnpm qa`. `/yolo` does not; it fixes failing or requested local checks without publish. `/build` and `/commit` update matching MDX and nearest README when behavior or commands change.
6. **Learn** — `/retro` identifies concrete improvements; changed conventions belong in existing durable documentation.

Tech skills (`fastify-v5`, `next-v16`, `vercel-react`) load when relevant. Playbooks do not add another backlog or quality policy.

The generator retains this catalog and rewrites its documentation pointers to the adopter’s local docs snapshot. `/release` is advisory and preserves the release tooling’s human gates.

## Checklists and evidence

Playbook bodies are numbered instructions (spec frontmatter, no Purpose/Handoff headings). Reviews use installed `review-dimensions.md`: correctness, callers, failure paths, clarity, architecture, security, performance, verification, and docs. Skip dimensions that are out of scope and say so. Debugging connects a reproduction to its cause and repeats the original scenario after a fix.

Shared references live in installed `workflow/references/`: `authoring.md`, `git-publish.md`, and `review-dimensions.md`. Record checks as passed, failed, not run, or not applicable with a reason. A type check alone does not verify a browser interaction. Documentation changes need proportionate checks; TDD remains explicit through `/tdd` or repository requirements. `/ship` is the only playbook that requires `pnpm qa`.

Further polish is later and small. `/tdd` stays opt-in. See [Skills](/docs/development/cursor-skills).

## Models (30 Aug 2026)

| Model | Use |
| --- | --- |
| **Cursor Grok 4.6** | Default for plan and implement. Grok 4.5 is a cheaper/faster picker option |
| **GPT-5.6 Sol** | Long-horizon work, large diffs, hard debugging |
| **Composer 2.5** | Fast multi-file execution after a reviewed plan |
| **Claude Code** (Opus 5) | Same `AGENTS.md` and `.agents/skills/` |

In-app chat (Fastify `/ai/chat`) defaults to Claude Haiku 4.5 — product model, not this page. Pass `model: "sonnet"` for Sonnet 4.6. See [Self-Hosted LLM](/docs/deployment/self-hosted-llm).

CodeRabbit reviews PRs against project standards. Local gates: Biome, ESLint, [pre-commit security](/docs/architecture/security).

Point agents at MDX: `Using the patterns from @api, add an endpoint…`

## Harness discovery

Checked 16 Sep 2026 during the agent-agnostic wiring change:

| Harness | Always-on contract | Skills | Status |
| --- | --- | --- | --- |
| Cursor | `AGENTS.md` plus short always-on `.mdc` pointers | `.agents/skills/` slash menu | Verified in this session (files present; slash `/plan` still the documented Cursor path) |
| Claude Code | `CLAUDE.md` first line `@AGENTS.md` | instructed to read `.agents/skills/` | Not verified (`/context` not run here) |
| Antigravity | `.agents/rules/always.md` | `.agents/skills/` | Not verified (no AGY session here) |
| Gemini CLI | `GEMINI.md` | `.agents/skills/` | Not verified |

## Related

- [Agent setup](/docs/development/cursor-setup) — optional Cursor indexing and MCP
- [Skills](/docs/development/cursor-skills) — catalog, install flags, inventory
- [AI](/docs/architecture/ai) — in-shell demo assistant
- Repo `.cursor/README.md` — Cursor file index (points here for how to work)
