# Copy to .env — see apps/mobile README. Placeholders only.
# Expo public vars (EXPO_PUBLIC_*) are exposed to the client bundle.

# -----------------------------------------------------------------------------
# Client (EXPO_PUBLIC_*)
# -----------------------------------------------------------------------------
# Simulator: Portless API. Physical device: LAN/tunnel URL instead (`.localhost` is the host).
EXPO_PUBLIC_API_URL=https://api.basilic.localhost
